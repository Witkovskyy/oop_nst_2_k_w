{
    "version": "0.2.1",
    "configurations": [
        {
            "type": "default",
            "project": "app",
            "projectTarget": "app.exe",
            "currentDir": "${./src}"
        }
    ]
}
